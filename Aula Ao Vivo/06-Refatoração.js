const compararValores = (valor1, valor2) => valor1 > valor2 ? 'uhul' : 'oh noo';
console.log(compararValores(12, 80));

// Exemplo 2
// Calcular area do triangulo

//function calcArea(base, altura){
  //return(base*altura) / 2;
//}

const calcArea = (base, altura) => (base*altura) /2; //com arrow function

console.log(calcArea(10,2));//

// Refatoração é basicamente a simplificação do nosso codigo!